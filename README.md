My Workspace congiguration file
==============

Configuration file includ:

shell rc scripts:
    bashrc
    alias
    env
    edit mode
    ... etc

screen rc script:
    screenrc

vim rc script:
    vimrc

global git configuration (username, alias)
    gitconfig
